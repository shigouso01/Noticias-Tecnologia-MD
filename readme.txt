# Manipulación de datos y ML clásico (TF-IDF, Chi-cuadrado, K-Means)
pip install scikit-learn

# Embeddings semánticos
pip install sentence-transformers

# BM25 — confirmado en PyPI: https://pypi.org/project/rank-bm25/
pip install rank-bm25

# NER y preprocesamiento lingüístico
pip install spacy
python -m spacy download es_core_news_sm

# Instalacion de Jupyter 
pip install jupyter